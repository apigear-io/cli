schema: apigear.rules/1.0
name: template rules
engines:
  cli: ">=v0.35"
features:
  - name: core
    scopes:
      - match: system
        documents:
          - { source: "hello.tpl", target: "hello.txt" }
