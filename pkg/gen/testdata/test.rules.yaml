features:
  - name: core
    scopes:
      - match: system
        documents:
          - { source: "system.name.tpl", target: "system.txt" }
      - match: modules
        documents:
          - { source: "module.name.tpl", target: "module.txt" }
