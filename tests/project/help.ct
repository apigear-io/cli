$ apigear project --help
The project command allows you to create a new project and manage it.

Usage:
  apigear project [flags]
  apigear project [command]

Aliases:
  project, prj

Available Commands:
  create      Create a new document inside current project
  edit        Edit a project in the default editor (vscode)
  import      Import a project from a git url into a local directory.
  info        Show project information
  init        Initialize a new project
  open        Open a project in studio
  pack        Pack a project
  recent      Show recent projects
  share       Share a project

Flags:
  -h, --help   help for project

Global Flags:
      --config string   config file (default is $HOME/.apigear.yaml)
      --dry-run         dry-run
  -v, --verbose         verbose output

Use "apigear project [command] --help" for more information about a command.


$ apigear project
Usage:
  apigear project [flags]
  apigear project [command]

Aliases:
  project, prj

Available Commands:
  create      Create a new document inside current project
  edit        Edit a project in the default editor (vscode)
  import      Import a project from a git url into a local directory.
  info        Show project information
  init        Initialize a new project
  open        Open a project in studio
  pack        Pack a project
  recent      Show recent projects
  share       Share a project

Flags:
  -h, --help   help for project

Global Flags:
      --config string   config file (default is $HOME/.apigear.yaml)
      --dry-run         dry-run
  -v, --verbose         verbose output

Use "apigear project [command] --help" for more information about a command.
